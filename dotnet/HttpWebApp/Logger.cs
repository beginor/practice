using System;
using System.Diagnostics;

namespace HttpWebApp {
	/*
	public class Logger : System.Web.Http.Common.ILogger {

		private log4net.ILog _log = log4net.LogManager.GetLogger(typeof(Logger));

		public void LogException(string category, TraceLevel level, Exception exception) {
			_log.DebugFormat("category: {0}, level: {1}, exception: {2}", category, level, exception);
		}

		public void Log(string category, TraceLevel level, Func<string> messageCallback) {
			_log.DebugFormat("category: {0}, level: {1}, message: {2}", category, level, messageCallback());
		}
	}
	*/
}